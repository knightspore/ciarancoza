name: deploy

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  deploy:
    name: build-and-deploy
    runs-on: debian-latest
    steps:
        - uses: actions/checkout@v4 
        - uses: oven-sh/setup-bun@v2
        - run: bun install
        - run: bun build
